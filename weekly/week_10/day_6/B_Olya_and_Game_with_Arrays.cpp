#include <bits/stdc++.h>

using namespace std;

int main() {
    int kase;
    cin >> kase;

    while (kase--) {
        int n;
        cin >> n;

        vector<int> vec(n);
        int mn = INF_Max;
        cout<<"M: "<<mn<<endl;

        for (auto &v : vec) cin >> v;

    }

    return 0;
}