#include <bits/stdc++.h>

using namespace std;

int main() {
    int kase;
    cin >> kase;

    while (kase--) {
        long long n, k;
        cin >> n >> k;

        vector<long long> nums;

        for (size_t i = 0; i < n; i++) {
            cin >> nums[i];
        }
    }

    return 0;
}