#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pi;

#define f first
#define s second
#define pb push_back
#define mp make_pair
#define rep(i, a, b) for (int i = a; i < b; i++)
#define ln printf("\n")
#define pln(val) cout << val << endl;

// Problems: Given an array of positive integers, find the subarrays that add up to a given number.

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    vi nums(0);
    int x, k = 7;
    while (cin >> x) {
        nums.pb(x);
    }

    return 0;
}