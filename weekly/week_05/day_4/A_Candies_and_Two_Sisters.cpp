#include <bits/stdc++.h>

using namespace std;

int main() {
    int kase;
    cin >> kase;

    while (kase--) {
        long long n;
        cin >> n;
        cout << (n - 1) / 2 << endl;
    }

    return 0;
}